<%= turbo_stream.update "stats" do %>
  <%= render "tackle_box_items/stats", fish_catches: @fish_catches %>
<% end %>

<%= turbo_stream.replace @fish_catch, @fish_catch %>

<%# this will render the helper method from /helpers/application_helper %>
<%= render_flash_stream %>